# GitHub MCP Server Dependencies
# Python >= 3.10 required
#
# IMPORTANT: It's recommended to use a virtual environment to avoid
# dependency conflicts with other packages (e.g., autotrain-advanced).
# 
# To create and use a virtual environment:
#   python -m venv .venv
#   .venv\Scripts\activate  # Windows
#   source .venv/bin/activate  # Linux/Mac
#   pip install -r requirements.txt

mcp>=0.9.0
pygithub>=2.1.1
python-dotenv>=1.0.0
setuptools>=61.0

